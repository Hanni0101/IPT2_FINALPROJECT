{
  "version": 2,
  "buildCommand": "cd dashboard && npm install && npm run build && cd ../server && npm install",
  "outputDirectory": "dashboard/dist",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/server/index.js"
    },
    {
      "src": "/uploads/(.*)",
      "dest": "/server/index.js"
    },
    {
      "src": "/(.*)",
      "dest": "/dashboard/dist/index.html"
    }
  ]
}
